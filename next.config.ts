import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['api.microlink.io','images.unsplash.com','images.pexels.com','www.pexels.com','res.cloudinary.com'],
  },
  /* config options here */
};

export default nextConfig;
